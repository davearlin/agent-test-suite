# Terraform state files - contain sensitive infrastructure state
*.tfstate
*.tfstate.*
*.tfstate.backup

# Terraform plan files - contain execution plans
*.tfplan
*.tfplan.*

# Terraform provider cache
.terraform/

# Variable files with sensitive data (actual values)
*.tfvars
*.auto.tfvars
*.auto.tfvars.json

# Example files should be kept for reference
!terraform.tfvars.example
!*.tfvars.example

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.auto.tfvars
*.auto.tfvars.json

# Include override files you do wish to add to version control
# !override.tf
# !override.tf.json

# Keep lock file for reproducible builds (this should be committed)